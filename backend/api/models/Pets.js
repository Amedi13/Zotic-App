/**
 * TOOO: 
 * Implement the Pets model with necessary fields and validations.
 */
class pets {
    constructor(id, ownerId, name, species, type, age_years, notes, createdAt) {
        this.id = id;
        this.ownerId = ownerId;
        this.name = name;
        this.species = species;
        this.type = type;
        this.age_years = age_years;
        this.notes = notes;
        this.createdAt = createdAt;
    }
}